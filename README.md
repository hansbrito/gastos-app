# gastos-app

Mobile dashboard for the WhatsApp expense tracker. Static site (no build) reading Supabase directly.
Auth: Supabase magic link, data locked by RLS to two emails. The anon key in index.html is public by design.

Deployed on DigitalOcean App Platform (free static site).
